JS TO TS CONVERSION 
====================

1. To run project
    ```sh
    $ yarn install
    $ yarn start
    ```

2. To run spectacle. Go to directory spectacle
  ```sh
    $ yarn install
    $ yarn start
    ```
